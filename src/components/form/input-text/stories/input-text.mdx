import { Meta } from '@storybook/blocks';

<Meta title="Form/Input Text/Demo" />

# Input text

```html
<p-input-text placeholder="Dot Paper" value="Paper"/>
<p-input-text placeholder="placeholder" label="Name" />
```

<p-input-text placeholder="Dot Paper" value="Paper"/>
<br />
<p-input-text placeholder="placeholder" label="Name" />
<br />
<br />

## Required

```html
<p-input-text placeholder="Full name is required" required label="Full name" />
```

<p-input-text placeholder="Full name is required" required label="Full name" />

## Disabled

```html
<p-input-text 
    placeholder="Name is required" 
    disabled 
    label="Name" 
/>
```

<p-input-text placeholder="Name is required" disabled label="Name" />

## Error

```html
<p-input-text label="Video Game" has-error/>
```

<p-input-text label="Video Game" has-error/>

## Dark

```html
<p-input-text 
    placeholder="placeholder" 
    label="Name" 
    dark
    block
    required
/>
<p-input-text dark  block value="GTA" label="Video Game" has-error/>
```

<div style={{padding: '10px', boxSizing: "border-box", width: "100%", background: "#41403e"}}>
    <p-input-text 
        placeholder="placeholder" 
        label="Name" 
        dark
        block
        required
    />
    <br />
    <p-input-text dark  block value="GTA" label="Video Game" has-error/>
</div>

## onChange

```html
<p-input-text id="video-game" label="Video Game" />
```

```javascript
const videoGame = document.querySelector('#video-game')

videoGame.addEventListener('change', (e: CustomEvent) => {
    console.log(e.detail)
})
```

<p-input-text id="video-game" label="Video Game" />

Answer: <span id="video-game-answer"></span>
