import { Meta } from '@storybook/blocks';

<Meta title="Form/Slider/Demo" />

# Slider

```html
<p-slider></p-slider>
<p-slider 
    block 
    min="10" 
    max="40"
></p-slider>
```

<p-slider></p-slider>

`p-slider` with block style : 

<p-slider block min="10" max="40"></p-slider>

<br />

## onChange event

```javascript
const slider = document.querySelector('#for-slider')

slider.addEventListener('change', (e: CustomEvent) => {
    sliderSpan.textContent = e.detail
})
```

```html
Value is <span id="for-slider-label">0</span>

<p-slider id="for-slider"></p-slider>
```

Value is <span id="for-slider-label">0</span>

<p-slider id="for-slider"></p-slider>

## Dark mode

```html
<p-slider dark min="10" max="40"></p-slider>
```

<div style={{padding: '10px', boxSizing: "border-box", width: "100%", background: "#41403e"}}>
    <p-slider dark min="10" max="40"></p-slider>
</div>