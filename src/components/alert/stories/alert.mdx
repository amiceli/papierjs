import { Meta } from '@storybook/blocks';

<Meta title="Components/Alert/Demo" />

# Alert

```html
<p-alert type="primary">PaperCss</p-alert>
<p-alert type="secondary">PaperCss</p-alert>
<p-alert type="success">PaperCss</p-alert>
<p-alert type="warning">PaperCss</p-alert>
<p-alert type="danger">PaperCss</p-alert>
```

<p-alert type="primary">PaperCss</p-alert>
<p-alert type="secondary">PaperCss</p-alert>
<p-alert type="success">PaperCss</p-alert>
<p-alert type="warning">PaperCss</p-alert>
<p-alert type="danger">PaperCss</p-alert>

## Closable

When you click on *close* `p-alert` will emit `close` event.

```html
<p-alert closable type="primary">PaperCss</p-alert>
<p-alert id="onClose" closable type="danger">Close me !</p-alert>
```

```javascript
// close event
element.addEventListener('close', () => {
    element.removeAttribute('closable')
    element.textContent = "Closed !"
})
```

<p-alert closable type="primary">PaperCss</p-alert>
<p-alert id="onClose" closable type="danger">Close me !</p-alert>

## Dark mode

<div style={{padding: '10px', boxSizing: "border-box", width: "100%", background: "#41403e"}}>
    <p-alert dark type="primary">PaperCss</p-alert>
    <p-alert dark type="secondary">PaperCss</p-alert>
    <p-alert dark type="success">PaperCss</p-alert>
    <p-alert dark closable type="warning">PaperCss</p-alert>
    <p-alert dark closable type="danger">PaperCss</p-alert>
</div>
