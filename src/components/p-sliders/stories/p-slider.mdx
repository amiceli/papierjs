import { Meta } from '@storybook/blocks';

<Meta title="Components/Slider/Demo" />

# Slider

```html
<p-sliders></p-sliders>
<p-sliders 
    block 
    min="10" 
    max="40"
></p-sliders>
```

<p-sliders></p-sliders>

`p-sliders` with block style : 

<p-sliders block min="10" max="40"></p-sliders>

<br />

## onChange event

```javascript
const slider = document.querySelector('#for-slider')

slider.addEventListener('change', (e: CustomEvent) => {
    sliderSpan.textContent = e.detail
})
```

```html
Value is <span id="for-slider-label">0</span>

<p-sliders id="for-slider"></p-sliders>
```

Value is <span id="for-slider-label">0</span>

<p-sliders id="for-slider"></p-sliders>

## Dark mode

```html
<p-sliders dark min="10" max="40"></p-sliders>
```

<div style={{padding: '10px', boxSizing: "border-box", width: "100%", background: "#41403e"}}>
    <p-sliders dark min="10" max="40"></p-sliders>
</div>